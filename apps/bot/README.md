# HYPEBOTX Bot

Discord bot runtime for slash commands, buttons, modals, tickets, orders, payments, joki queue, and notifications.

Run locally:

```powershell
npm run dev --workspace=apps/bot
```

The bot can still read root `.env`/`.env.local` during migration, but production should prefer app/server environment variables.
