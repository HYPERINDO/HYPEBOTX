# HYPEBOTX Dashboard Backend

Express API for Discord OAuth login, httpOnly session cookies, dashboard role checks, shared JSON data access, and audit logging.

Start locally:

```powershell
npm run dev --workspace=apps/dashboard-backend
```

Secrets live here or in server environment variables, never in the frontend.
