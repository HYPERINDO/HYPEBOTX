# HYPEBOTX Dashboard Frontend

React dashboard UI for Owner, Admin, and Penjoki roles. Login is handled by the backend Discord OAuth endpoint and all API requests use `withCredentials`.

Start locally:

```powershell
npm run dev --workspace=apps/dashboard-frontend
```
